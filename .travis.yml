language: cpp
sudo: false
dist: trusty

notifications:
  email:
    on_success: never
    on_failure: always

matrix:
  include:
  - os: osx
    osx_image: xcode9.3
    compiler: clang
    env: CXX=clang++

script:
- brew upgrade cmake
- cd build
- cmake ..
- make
- test/Test
